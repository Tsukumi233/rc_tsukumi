# AI 使用说明

## AI 帮了什么

AI 协助分析系统边界、重复投递和崩溃窗口，编写 API、Worker、请求校验、模拟供应商和测试。它还比较了 pg-boss 与 BullMQ，提出并实现了 BullMQ + 持久化 Redis 方案。

类型检查、集成测试、真实 HTTP E2E、Docker 演示、Worker SIGKILL、过期令牌写回和 Redis 重启验证由 AI 执行，结果见 [验证记录](docs/VALIDATION.md)。这些结果不代表我已亲自逐行审阅全部代码，也不能当成生产 SLA。

## 我没有采纳什么

AI 初版为了减少中间件，建议用 PostgreSQL 自写抢占、租约回收和轮询。我要求改用能覆盖需求的成熟开源库：通知投递是通用需求，自己维护队列会增加代码和故障处理成本。

工作流引擎、Kafka、多库 Outbox 等也出现在方案比较中，但没有进入实现。它们并非都经过了“AI 建议、我否决”的过程。

## 哪些判断由我做出

我选择 TypeScript、pnpm 和 Hono，希望减少接口开发中的重复工作，并用类型、校验和 OpenAPI 保持接口与文档一致。

我提出开源优先的选型标准：看需求覆盖、代码量、仓库复杂度、社区维护和实际性能，不能只看少部署几个组件。Worker 一致性和为什么不使用队列框架，也是我要求重点解释的问题。我还要求增加行为级 E2E：从真实 HTTP 入口提交，检查供应商实际收到的请求，并验证进程故障后的恢复。

BullMQ 的具体选型与配置由 AI 提出并实现。受理何时算成功、何时可能重复投递、失败如何结束，以及历史任务保留的成本，已写入[设计与取舍](docs/DESIGN.md)，作为评审实现的依据。
