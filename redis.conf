# Local Compose only; host port is bound to 127.0.0.1. Use ACL/TLS on a private network in production.
bind 0.0.0.0
protected-mode no
dir /data
appendonly yes
appendfsync always
no-appendfsync-on-rewrite no
maxmemory 256mb
maxmemory-policy noeviction
save 900 1
